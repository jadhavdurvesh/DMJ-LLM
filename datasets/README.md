# Datasets

This directory is intentionally kept empty.

Training datasets are generated by the **DMJ Dataset Builder** project.

For training, place the generated dataset (for example `dmj_dataset_v1.0.0.jsonl`) in this directory temporarily or configure the path in `training/config.py`.

Large datasets are not tracked in this repository.
